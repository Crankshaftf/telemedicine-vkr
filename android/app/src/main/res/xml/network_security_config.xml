<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN (HTTP) + internet (HTTPS tunnel / cloud) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
